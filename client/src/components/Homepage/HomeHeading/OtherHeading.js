import React from "react";
import PropTypes from "prop-types";
import { Container } from "semantic-ui-react";

const OtherHeading = ({ mobile }) => (
  <Container text>

    {/* <Header /> */}
    
  </Container>
)

OtherHeading.propTypes = {
  mobile: PropTypes.bool,
}

export default OtherHeading;